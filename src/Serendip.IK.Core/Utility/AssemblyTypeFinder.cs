namespace Serendip.IK.Utility
{
    public static class AssemblyTypeFinder
    {
        //public static Dictionary<string, Type> dictionary = new Dictionary<string, Type>();
        //static AssemblyTypeFinder()
        //{
        //    var assemblies = AppDomain.CurrentDomain.GetAssemblies().Where(x => x.FullName.StartsWith("BA"));
        //    foreach (var assembly in assemblies)
        //    {
        //        var types = assembly.GetTypes();
        //        for (int j = 0; j < types.Length; j++)
        //        {
        //            if (!string.IsNullOrEmpty(types[j].FullName) && !dictionary.ContainsKey(types[j].FullName))
        //            {
        //                dictionary.Add(types[j].FullName, types[j]);
        //            }
        //        }
        //    }
        //}
    }
}
